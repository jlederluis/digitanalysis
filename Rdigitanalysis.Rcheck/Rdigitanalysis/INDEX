Benford_stimulation     Stimulatse N Benford distribution datasets with
                        matching digit places as the observed dataset.
                        SHOULD I EXPORT THIS???!!!
Benford_table           Generates contingency table for Benford
                        distribution (a.k.a expected digit frequency
                        under Benford's Law). Columnn are digit place
                        in increasing order, and rows are the digits (0
                        to 9) in increasing order
DigitAnalysis-class     The class object for RDigitAnalysis package
align_digits            Align the digits from the left/right of a data
                        column and update it to the specified data
                        frame
all_digits_test         Performs all-digit-place two-way chi square
                        test vs Benford’s Law
break_by_category       Breaks the data on specfied column
chi_square_gof          Perform chi square test for goodness of fit
                        test
combine_by_columns      Fetches the merged rows of all data columns
                        aligned-right digits. MIGHT BE USEFUL IN ADT!
compute_percent_rounded_digits
                        Compute the percent of rounded digits in a
                        given dataframe. Removes all 0 entries before
                        begin.
counts_observed         Find the frequency of terminal digit pairs
                        occuring in the data being analzyed
digit_pairs_test        Performs terminal digit pair binomial test vs
                        uniform distribution (Benford’s Law)
drop_first_digit_places
                        Remove first digit from a
                        left-aligned/right-aligned digits table
drop_last_digit_places
                        Remove last digit from a
                        left-aligned/right-aligned digits table
find_percent_repeats    Finds the percent of repeats in the given data
                        based on given definition of a repeat (what
                        columns need to match)
get_benford_mean        Obtains Benford mean in each digit place after
                        specifying omitting 0 and 5 or not
get_data_columns        Grab all data column names when data_columns =
                        'all'
get_df                  Compute the degrees of freedom for the input
                        chi square test table
get_expected_mean       Obtains expected mean conforming to Benford's
                        Law from input 'data' based on the number of
                        digits in each left-aligned digit place
get_observed_mean       Obtains the observed mean from input 'data'
get_p_value             Obtains p_values for comparing with Monte Carlo
                        stimulated datasets
get_round_unround_digitdata
                        Creates two two copies of digitdata: • A copy
                        with only round entries in the specified
                        numeric column • A copy with only unround
                        entries in the specified numeric column Helper
                        function for 'unpack_round_numbers_test'.
grab_desired_aligned_columns
                        Fetches the left-aligned/right-aligned data
                        columns for analysis, drop the first and last
                        digit places if desired
high_low_by_digit_place
                        Computes the high low digit binomial test by
                        digit place for desired data columns. Helper
                        function for 'high_low_test'
high_low_test           Performs high to low digit tests vs probability
                        of high to low digits by Benford's Law via
                        binomial test
input_check             Asserts the input to user accessible functions
                        in this package are of correct type and form.
load_Benford_table      Loads Benford contingency table
make_aligned_data       Create the dataframe with the left/right
                        aligned digits of each data column to be
                        analyzed->'left_aligned'/'right_aligned' of the
                        class
make_class              Create an object instance for 'DigitAnalysis'.
                        Parse and clean the data for digit analysis.
make_cleaned_data       Clean up the numeric columns of dataframe
make_numeric_data       Create dataframe for only the numeric columns
                        of the cleaned data
make_raw_data           Load raw dataframe from file or from passed-in
                        dataframe
max_length              Find the length of the largest number in a data
                        column
obtain_observation      Parse cleaned digits table from
                        'parse_digit_places' to obtain observation
                        table for chi square test
padding_test            Performs padding test vs stimulations of
                        Benford conforming datasets via percentile
parse_contigency_table
                        Parse the contigency table to have exclusively
                        the desired digits and digit places
parse_digit_places      Obtain exclusively the desired digit places for
                        left aligned digits table
repeat_test             Performs repeat test or sector effect test
rounding_test           Performs rounding test vs uniform distribution
                        across categories in a specified data column
sector_test             Performs sector test to analyze uneven
                        distribution of percent repeats across sectors
                        (supposed to be uniform). A wrapper function
                        for 'repeat_test'.
single_column_aligned   Fetches the left/right aligned table for a
                        single numeric data column
unpack_round_numbers_test
                        Performs unpacking unround number test by
                        performing all-digit place two-way chi square
                        test vs Benford’s Law. A wrapper function for
                        'all_digit_test'.
unpacking_round_number_split
                        Get the indexes of round and unround entries in
                        the specified column in 'digitdata'
